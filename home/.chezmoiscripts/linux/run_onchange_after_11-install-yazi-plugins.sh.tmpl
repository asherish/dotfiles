#!/bin/bash
# package.toml hash: {{ include "private_dot_config/yazi/package.toml" | sha256sum }}

if command -v ya >/dev/null 2>&1; then
  echo "Installing yazi plugins..."
  ya pkg install
else
  echo "ya not found, skipping yazi plugin install"
fi

