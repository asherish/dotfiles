# Environment variables for all bash sessions (login shells, sourced by .bashrc for non-login).

export EDITOR=nvim
export VISUAL=nvim

# Ensure ~/.local/bin is in PATH
case ":$PATH:" in
    *":$HOME/.local/bin:"*) ;;
    *) export PATH="$HOME/.local/bin:$PATH" ;;
esac
