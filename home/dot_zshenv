# Environment variables for all zsh sessions (login, interactive, scripts).

export EDITOR=nvim
export VISUAL=nvim

# Ensure ~/.local/bin and ~/bin are in PATH
typeset -U path
path=("$HOME/.local/bin" "$HOME/bin" $path)
